# M03–M05 Stepik-flow / evidence fix — production summary

**Дата:** 12 сентября 2026 года  
**База:** `main` @ `34c137ea8e1a11e3886da9ea6d78d068ee845831`  
**Ветка:** `production/m03-m05-stepik-flow-fix-v1`  
**Нормативное основание:** `D-2026-09-12-STEPIK-FLOW`  
**Human validation:** `NOT PERFORMED`

## 1. Граница fix-batch

Исправления ограничены `M03-L02`, `M04-L02`, `M05-L02` и одной строкой Coverage Matrix, которая задаёт evidence `M04-L02`.

Не менялись:

- Module ID;
- Lesson ID;
- Exercise ID;
- Check ID;
- Asset ID;
- competency placement;
- уровни самостоятельности;
- INTRO / PRACTICE / INDEPENDENT / REUSE;
- PRIMARY / BACKUP;
- общая архитектура модулей.

## 2. M03-L02

Исправлено:

- перед `E01` добавлен learner-facing advisory: сначала выполнить самостоятельную попытку и сохранить естественный след, затем открывать `C01`;
- объяснена причина: критерии `C01` способны подсказать ход решения и снизить ценность самостоятельной проверки;
- отсутствие hard gate Stepik больше не описывается как blocker;
- если ранний просмотр `C01` содержательно загрязнил попытку, используется уже существующий re-check `R1`, а исходная ситуация остаётся PRACTICE;
- B1 temporal evidence, B2, B5, B6 не ослаблены;
- `M03-L02-A03.png` и metadata подтверждены как `READY`; старые operational-формулировки `placeholder / OWNER CAPTURE REQUIRED / BLOCKED` удалены из `lesson.md`, `stepik-plan.md`, `author-notes.md`.

Исторические production/synthetic-снимки предыдущего batch не переписываются задним числом; их старые blocker-формулировки описывают состояние на момент того прогона и superseded текущим asset metadata и настоящим fix-batch.

## 3. M04-L02

Исправлён ложный PASS B3.

Теперь level-3 evidence требует фактически выполненного действия до передачи:

- при обезличивании/замене — реально подготовленную безопасную версию;
- при безопасном пересказе — реально написанный безопасный пересказ;
- при отказе — отсутствие upload и зафиксированный до передачи отказ с краткой причиной;
- передача как есть допустима только при самостоятельном обосновании необходимости и допустимости.

Перечень «я бы удалил / заменил» без выполненного действия не даёт PASS. Заявление об отказе после загрузки не даёт PASS. Privacy-before-transfer сохранена.

Синхронизированы `lesson.md`, `stepik-plan.md`, `lesson-card.md` и строка M04-L02 в Coverage Matrix.

## 4. M05-L02

Исправлено:

- перед B7 добавлен learner-facing advisory: сначала самостоятельно сформулировать задачу, выполнить live generation, сохранить исходный запрос и результат, затем переходить к Part 2;
- объяснена причина: ранний Part 2 может подсказать направление B8-изменения;
- staged views `part1`/`part2` сохранены как способ уменьшить случайную подсказку, но не изображаются техническим hard gate;
- если ранний Part 2 содержательно загрязнил B8, исходная B8-ситуация становится PRACTICE, а для самостоятельного C02 используется alternate scenario;
- B7 не аннулируется автоматически, если собственное описание + live generation доказаны;
- B8 по-прежнему требует реальный edit существующего исходника; новая генерация с нуля не заменяет B8;
- BACKUP B8 остаётся узким background-edit, но техническая ограниченность BACKUP не превращает любую иную разумную правку ученика в «неправильный ответ»;
- добавлены recovery-сценарии для PRIMARY и узкого BACKUP без новых Exercise/Check/Asset ID.

## 5. Regression contract

Проверить перед PR и повторно в отдельном adversarial review:

- соответствие `D-2026-09-12-STEPIK-FLOW`;
- learner-facing advisory находится непосредственно перед рискованным переходом;
- advisory не раскрывает содержательные действия B1/B2/B5/B6 или конкретную B8-правку;
- `M03-L02-C01` по-прежнему требует независимые evidence B1/B2/B5/B6;
- `M04-L02-C01` не принимает намерение вместо действия;
- privacy действует до передачи;
- `M05-L02-C01` требует live B7;
- `M05-L02-C02` требует real edit существующего исходника;
- BACKUP не становится содержательным шаблоном ответа;
- A03 operational status согласован с фактическим `READY`;
- нет новых скрытых цифровых требований;
- human validation остаётся `NOT PERFORMED`.

## 6. Граница принятия

Этот fix-batch не является cumulative audit M00–M05 и не объявляет M00–M05 окончательно принятыми. Отдельный cumulative audit выполняется после завершения настоящего branch → PR → critical audit → merge workflow.
